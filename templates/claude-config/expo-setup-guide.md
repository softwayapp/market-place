# Expo Project Setup Guide

> 🤖 **Auto-generated by /init:expo**
>
> **실행 방법**: 아래 중 하나를 말하면 자동으로 설정이 시작됩니다:
> - "실행해" / "설정해" / "셋업해" / "시작해"
> - "run it" / "execute" / "start setup"

## 📊 Project Analysis Results

### Detected Configuration
- **Project Name**: {{PROJECT_NAME}}
- **Expo Version**: {{EXPO_VERSION}}
- **TypeScript**: {{TYPESCRIPT}}
- **Router**: {{ROUTER}}
- **UI Library**: {{UI_LIBRARY}}

## ⚠️ Important Notice

> **⚠️ 설정 중 주의사항**
> 
> 설정 과정 중에는 **Git 관련 작업을 수행하지 마세요**:
> - ❌ `git add`, `git commit`, `git push` 등 Git 명령어 실행 금지
> - ❌ Git 상태 확인 (`git status`)도 피하세요
> - ✅ 설정이 완전히 끝난 후에만 Git 작업을 들어갈 수 있습니다.

---

## 🔧 Setup Tasks

### 1. Dependencies to Install

```bash
# Core dependencies
{{CORE_DEPS}}

# NativeWind for Expo
npx expo install nativewind
npx expo install tailwindcss
npm install --save-dev tailwindcss@3.3.2

# Navigation
{{NAV_DEPS}}

# Development tools
{{DEV_DEPS}}
```

### 2. Folder Structure to Create (Atomic Design Pattern)

```
components/
├── atoms/              # 기본 UI 요소 (Button, Input, Text, Icon 등)
├── molecules/          # atoms 조합 (SearchBar, FormField, Card 등)
├── organisms/          # molecules 조합 (Header, Footer, Navigation 등)
├── templates/          # 페이지 레이아웃 템플릿
└── index.ts           # Barrel exports

hooks/                 # 커스텀 React 훅
utils/                 # 유틸리티 함수
types/                 # TypeScript 타입 정의
schema/                # Validation schemas (Zod, Yup 등)
libs/                  # 외부 라이브러리 설정 및 래퍼
stores/                # 전역 상태 관리 (Zustand, Redux 등)
styles/                # 전역 스타일, 테마, Tailwind 설정
constants/             # 앱 상수 (colors, sizes, API endpoints 등)
mock/                  # 목 데이터 및 fixtures
assets/
└── fonts/             # Pretendard 폰트 파일
```

### 3. Fonts to Download

- Pretendard-Regular.woff2
- Pretendard-Medium.woff2
- Pretendard-SemiBold.woff2
- Pretendard-Bold.woff2

**Target**: `assets/fonts/`

### 4. Configuration Files

자동으로 생성되는 설정 파일:

- ✅ `tailwind.config.js` - NativeWind 설정
- ✅ `babel.config.js` - NativeWind plugin
- ✅ `metro.config.js` - CSS support
- ✅ `styles/global.css` - Tailwind directives
- ✅ `constants/theme.ts` - 테마 상수

## ⚡ Execution Trigger

**아래 중 하나를 말하면 자동으로 시작됩니다**:

### 한국어
- "실행해" / "실행해줘"
- "설정해" / "설정해줘"
- "셋업해" / "셋업해줘"
- "시작해" / "시작해줘"
- "진행해" / "진행해줘"

### English
- "run it" / "run setup"
- "execute" / "execute setup"
- "start" / "start setup"
- "go" / "proceed"

---

*이 파일은 설정이 완료되면 프로젝트 문서로 자동 교체됩니다*
