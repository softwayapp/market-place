# React Project Setup Guide

> 🤖 **Auto-generated by /init:react**
>
> **실행 방법**: 아래 중 하나를 말하면 자동으로 설정이 시작됩니다:
> - "실행해" / "설정해" / "셋업해" / "시작해"
> - "run it" / "execute" / "start setup"

## 📊 Project Analysis Results

### Detected Configuration
- **Project Name**: {{PROJECT_NAME}}
- **React Version**: {{REACT_VERSION}}
- **TypeScript**: {{TYPESCRIPT}}
- **Build Tool**: {{BUILD_TOOL}}
- **UI Library**: {{UI_LIBRARY}}
- **Router**: {{ROUTER}}

## 🔧 Setup Tasks

### 1. Dependencies to Install

```bash
# Essential libraries
pnpm add @tanstack/react-query zustand react-hook-form zod axios date-fns framer-motion lucide-react tailwind-merge

# Dev dependencies
pnpm add -D @tanstack/react-query-devtools

# Tailwind CSS (if not installed)
pnpm add -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

# shadcn/ui (optional - interactive)
npx shadcn-ui@latest init -d
```

### 2. Folder Structure to Create (Atomic Design Pattern)

```
src/
├── components/
│   ├── ui/                # shadcn/ui components
│   ├── atoms/             # 기본 UI 요소 (Button, Input, Text, Icon 등)
│   ├── molecules/         # atoms 조합 (SearchBar, FormField, Card 등)
│   ├── organisms/         # molecules 조합 (Header, Footer, Navigation 등)
│   ├── templates/         # 페이지 레이아웃 템플릿
│   └── index.ts          # Barrel exports
├── hooks/                # 커스텀 React 훅
│   ├── queries/          # TanStack Query hooks
│   └── mutations/        # TanStack Mutation hooks
├── stores/               # Zustand 전역 상태 관리
├── lib/                  # 라이브러리 설정 및 유틸리티
│   ├── axios.ts          # Axios instance & interceptors
│   └── utils.ts          # Helper functions (cn, etc.)
├── types/                # TypeScript 타입 정의
├── schema/               # Zod validation schemas
├── constants/            # 앱 상수 (colors, sizes, API endpoints 등)
├── styles/               # 전역 스타일, 테마
└── assets/
    └── fonts/            # Pretendard 폰트 파일
```

### 3. Fonts to Download

- Pretendard-Regular.woff2
- Pretendard-Medium.woff2
- Pretendard-SemiBold.woff2
- Pretendard-Bold.woff2

**Target**: `public/fonts/` (for Vite) or `src/assets/fonts/`

### 4. Configuration Files

자동으로 생성되는 설정 파일:

- ✅ `tailwind.config.js` - Tailwind CSS 설정
- ✅ `postcss.config.js` - PostCSS 설정
- ✅ `src/styles/globals.css` - Tailwind directives + font imports
- ✅ `src/lib/utils.ts` - cn() utility for Tailwind classes
- ✅ `src/lib/axios.ts` - Axios instance with interceptors
- ✅ `src/providers/QueryProvider.tsx` - TanStack Query setup
- ✅ `public/fonts/pretendard.css` - Font face declarations

### 5. Provider Setup

TanStack Query Provider 설정:

```tsx
// src/providers/QueryProvider.tsx
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { ReactQueryDevtools } from '@tanstack/react-query-devtools';

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 1000 * 60 * 5, // 5 minutes
      retry: 1,
    },
  },
});

export function QueryProvider({ children }: { children: React.ReactNode }) {
  return (
    <QueryClientProvider client={queryClient}>
      {children}
      <ReactQueryDevtools initialIsOpen={false} />
    </QueryClientProvider>
  );
}
```

그리고 `main.tsx`나 `App.tsx`에서 wrapping:

```tsx
import { QueryProvider } from './providers/QueryProvider';
import './styles/globals.css';

function App() {
  return (
    <QueryProvider>
      {/* Your app */}
    </QueryProvider>
  );
}
```

## 📚 Library Quick Reference

### State Management Strategy
- **Server State** → TanStack Query (API data, caching)
- **Global Client State** → Zustand (user preferences, UI state)
- **Local State** → useState (component-specific)
- **Form State** → React Hook Form (form data)

### Key Libraries
- **@tanstack/react-query**: Server state management with caching
- **zustand**: Lightweight global state management
- **react-hook-form**: Performant form handling
- **zod**: Runtime validation and type inference
- **axios**: HTTP client with interceptors
- **date-fns**: Modern date utilities
- **framer-motion**: Declarative animations
- **lucide-react**: Beautiful icon library
- **tailwind-merge**: Merge Tailwind classes intelligently

## ⚡ Execution Trigger

**아래 중 하나를 말하면 자동으로 시작됩니다**:

### 한국어
- "실행해" / "실행해줘"
- "설정해" / "설정해줘"
- "셋업해" / "셋업해줘"
- "시작해" / "시작해줘"
- "진행해" / "진행해줘"

### English
- "run it" / "run setup"
- "execute" / "execute setup"
- "start" / "start setup"
- "go" / "proceed"

---

*이 파일은 설정이 완료되면 프로젝트 문서로 자동 교체됩니다*
